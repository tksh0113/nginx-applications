server {
    listen       8080; # non-rootユーザーの場合は Well Known Ports (0-1023) でないものを指定。
    server_name  localhost;

    root   /usr/share/nginx/html;
    index index.html;

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}
